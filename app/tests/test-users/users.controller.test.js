const sinon = require('sinon');
const chai = require('chai');
const expext = chai.expect;

describe('register user endpoint', () => {

    afterEach(done => {
        sandbox.restore();
        done();
    });

    it('should call method a user in the database', userCreated =>{
        



    });



});